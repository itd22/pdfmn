# pdfmn
pdf manifest for lists of pdf

Run `pdftui` (or `./run_app.sh`) for the interactive ncurses TUI
(default). Use `--tui=no --action ...` for the classic one-shot CLI.
See `doc/DIAGRAM.md` for architecture and `doc/pipx.md` for install/usage.
